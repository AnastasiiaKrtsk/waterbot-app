import styled from "styled-components";

export const StyledAddWaterBtn = styled.button`
  border: none;
  background-color: transparent;
  color: var(--dark-blue);

  font-size: 18px;
  font-style: normal;
  font-weight: 500;
  line-height: 24px;
  width: 114px;
  height: 24px;

  &:hover {
    color:var(--orange);
  }
`;
